# Pump state assets

将归一化后的水泵状态图片放在此目录：

- `pump-gray.png`
- `pump-green.png`
- `pump-blue.png`
- `pump-orange.png`
- `pump-red.png`

M1 约定：

- 设计画布：`512 × 720`
- RGBA 透明背景
- 五张图片使用完全相同的设备边界
- 状态切换时不得改变组件几何位置

如果 PNG 尚未放入，编辑器会回退到仓库中的同源 SVG 状态图。

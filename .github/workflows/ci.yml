name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply editor refinement
        if: github.head_ref == 'agent/refine-editor-layout-and-auto-connect'
        run: python scripts/refine_editor_layout.py

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Commit verified implementation
        if: github.head_ref == 'agent/refine-editor-layout-and-auto-connect'
        shell: bash
        run: |
          rm scripts/refine_editor_layout.py
          cat > .github/workflows/ci.yml <<'YAML'
          name: CI

          on:
            push:
              branches: [main]
            pull_request:
              branches: [main]

          jobs:
            verify:
              runs-on: ubuntu-latest
              steps:
                - name: Checkout
                  uses: actions/checkout@v4

                - name: Set up Node.js
                  uses: actions/setup-node@v4
                  with:
                    node-version: 22

                - name: Install dependencies
                  run: npm install

                - name: Build
                  run: npm run build

                - name: Lint
                  run: npm run lint
          YAML
          git config user.name "scada-editor-bot"
          git config user.email "scada-editor-bot@users.noreply.github.com"
          git add src/App.tsx src/renderer/SceneRenderer.tsx src/scene/scene-bounds.ts src/workbench.css .github/workflows scripts
          git commit -m "refactor: simplify editor layout and enable automatic connections"
          git push origin "HEAD:${{ github.head_ref }}"

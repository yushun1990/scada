# 组件编辑器后续小批次

基线：用户已将上一轮调整合入 `main`（`3b3106d`）；PR #196 另含图层标题简化。每个批次单独记录实现、验证和提交，后续会话从“下一步”继续，不重新扩大为一轮整体重设计。

## 批次与验收

| 批次 | 范围 | 完成条件 | 状态 |
| --- | --- | --- | --- |
| U0 | 修复 PR #196 检查 | 两个菜单生命周期断言稳定，PR 原有检查全部通过 | 已完成：`331b150`，远端六项通过 |
| U1 | 恢复素材拖入画布；“上传资源”命名 | 图元、文本、SVG、图片真实拖拽可创建，坐标正确，可撤销，预览禁止修改 | 本地验证通过，待提交 |
| U2 | 组合命中优先与行内四个排序按钮 | 画布默认整组选择、拖动；内部编辑入口明确；置顶、上移、下移、置底直接可达 | 待 U1 |
| U3 | 组件与图层的检查器作用范围 | 两个固定入口，切换不丢图层选择；属性、方法、事件都明确所属对象，只呈现实际能力 | 待 U2 |
| U4 | 侧栏边缘折叠与紧凑画布 | 移除页头布局菜单；边缘按钮可收起和展开；尺寸偏好保留；压缩工具栏、扩大可见绘图区 | 待 U3 |

每个批次以一项可演示交互为交付单位。实现后仅运行相关模型和浏览器检查，再记录提交与下一步。遇到使用额度或会话中断，已通过的批次不回滚，未通过部分留明确复现步骤；不把“本地通过”记成“远端已通过”。

## 当前结论

- PR #196 的 Component toolbar 检查在菜单挂载前统计项目，得到 `0 !== 8`；Shell 检查在菜单挂载前判断显示/隐藏入口，走错分支。修正等待顺序，保留项目数量和面板几何断言。
- 素材放置代码仍查找 `.component-editor-shell`，当前容器已是 `.component-studio-shell`，导致画板未安装 drop 监听。U1 应改为显式画板引用，避免再次依赖外壳类名。
- 当前“方法／事件”无论选中什么都编辑组件公开契约，容易让用户以为是在编辑图层。U3 先明确组件与图层作用范围，不能用一套标题掩盖两种不同能力；也不能给普通图元虚构可调用方法或事件。
- 组合采用画布默认整组选择；内部图层编辑入口、画布尺寸解释待用户偏好反馈。扩大可见空间与更改组件设计尺寸需要区分，旧组件尺寸不能被全局样式改写。

## 续接点

U0 已完成：两项菜单检查连续三轮通过，图层导航通过，提交 `331b150` 已推到 PR #196，六项远端检查全部成功。当前分支 `codex/component-authoring-followup` 从该提交续接。U1 的 build、lint 与真实素材拖拽检查已通过，包含图元、文本、SVG、PNG、坐标、撤销重做、重载和预览门禁。接下来 U2：默认画布整组选中；从左侧明确选择内部图层后可编辑该层；四个排序按钮直接显示。

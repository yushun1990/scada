# M6.3.1.1 Canvas Hit Testing & Visual Design Space

Status: implemented on `main` · CI #387 ✅ · manual browser smoke pending.

This corrective slice follows the first M6.3.1 manual browser smoke. It fixes two authoring problems exposed by real Layer interaction.

## Problems found

1. Newly created visual Layers start in the same local position. When sibling visuals overlap, normal Konva hit testing resolves the topmost sibling, so repeatedly clicking the same overlap appears to make the last-created Layer impossible to leave.
2. The Component Workbench artboard reused `ComponentDefinition.size.defaultWidth/defaultHeight` as its private visual design coordinate system. A small SCADA instance default such as `96 × 72` therefore also produced a tiny component-development artboard.

The second problem is an architectural coupling, not only a zoom issue. Public instance sizing constraints belong to SCADA scene usage; private component visual authoring needs its own stable coordinate space.

## Direction

```text
ComponentDefinition.size
  defaultWidth / defaultHeight
  minWidth / minHeight
        ↓
SCADA instance creation / resize constraints

ComponentVisualDefinition.designSize
        ↓
private component visual coordinate system
        ↓
Component Workbench authoring
        ↓
Composite renderer scales into an instance box
```

## Implemented

- `ComponentVisualDefinition` is now visual schema v2 and owns a private `designSize`.
- New composite visuals use an independent `480 × 360` design space.
- Existing v1 visuals migrate using the component's current default instance width/height so existing Layer coordinates keep their previous meaning.
- After migration, later changes to `ComponentDefinition.size` no longer redefine the private visual coordinate system.
- `CompositeComponentVisualRenderer` scales from `visual.designSize` into the requested render box rather than consuming public instance defaults as its source coordinate system.
- Component Workbench fits the visual design space into a large editing artboard (up to roughly `720 × 520`) instead of refusing to upscale small components.
- The canvas status now distinguishes `设计空间` from `实例默认` size.
- Repeated clicks at the same point cycle through overlapping Layer hits during a short hit-cycle window, instead of permanently resolving to only the topmost sibling.
- A selected Layer's transparent drag hit area now renders before its own visible content and children, so selecting a Group does not mask child-layer hit testing.

## Boundaries

No change to Runtime values, bindings, behaviors, public Actions / Events / Anchors, SceneDocument, 2.5D projection, Visual Rules, Animation or Script.

## CI verification

Push CI #387 completed successfully on `0373cd74961ebdfcb9f5f557e7adbcb8a4e91ef4`.

Verified:

- dependency installation
- TypeScript / Vite Build
- oxlint
- Studio UI primitive audit

## Manual browser smoke

Verify the component reported during M6.3.1 smoke:

- the white Component Workbench artboard is now large enough for direct manipulation even when SCADA instance default size is small
- status bar shows private design-space size separately from instance default size
- changing component default/minimum instance sizes does not resize or redefine the private visual coordinate system
- repeatedly click the same overlapping SVG/Vector region and confirm selection cycles through the hit Layers
- select a Group and confirm clicking a visible child can select that child rather than the Group hit area masking it
- drag / resize / rotate selected Layers and confirm Inspector geometry still updates
- save/reopen the existing v1 component and confirm migrated visual geometry remains intact
